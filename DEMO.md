# 从一份清单到正文与 SI 交付

这是 `paper-si-workflow` 的入门演示。下面的文献和平台状态均为虚构，用于说明操作与状态判断；不是实际下载记录，也不是成功率证据。

## 1. 安装和第一次提问

从 [v0.1.0 发布页](https://github.com/BinhuanQiu/paper-si-workflow/releases/tag/v0.1.0) 下载 Source code (zip)。将其中的 `skills/paper-si-workflow` 完整文件夹安装到客户端的技能目录，保留 `references`、`assets` 和 `agents` 子目录。

在支持该 Skill 的客户端中，可以这样提出任务：

```text
使用 $paper-si-workflow 处理我的文献任务表。
正文和全部 SI 都需要，按照表中的主题目录和短标题归档。
已有论文只读，核对后可以复制，不移动、不覆盖。
先复用已有文件和公开渠道；缺项允许通过我已登录的科研通账号求助。
本次新增积分支出上限为 60 分，不加价、不置顶。
遇到登录或验证码让我接手。
```

60 分只是这段示例的用户预算，不是平台价格。实际求助与下载费用必须读取当时页面。

安装 Skill 不等于浏览器已经连接。助手应先说明当前哪些工具可用；没有浏览器控制时，不应假装已发布求助。没有后台调度器时，不应假装关闭对话后还在等待收件。

## 2. 一组混合任务应该如何处理

假设助手已经通过相应工具取得下列证据。这些是演示条件，而非真实文件检查。

| 任务 | 当前证据 | 下一步 | 此刻的状态 |
|---|---|---|---|
| A | 正文正确；官方列出两份 SI，两份均已保存并确认身份 | 复用并归档，不再求助 | 正文已核对，SI 完整 |
| B | 正文正确；官方列出三份 SI，只拿到两份 | 只处理缺失的第三份 | SI 部分下载 |
| C | 正文正确；仅找到一份 SI，但官方页面受限，无法核对总数 | 保留文件和来源，继续寻找完整度证据 | SI 已取得，完整性未确认 |
| D | 正文正确；官方记录明确确认没有 SI | 保存“无 SI”的来源和核对日期 | SI 确认无 |
| E | 正文已有待应助求助；SI 尚未求助 | 保留正文请求，单独处理 SI | 正文待应助，SI 待处理 |
| F | 上次发布 SI 时页面超时，无法确定是否成功 | 先查“我的求助”，不立即重发 | 发布结果不明 |
| G | SI 仍缺失，用户接受缺项并结束任务 | 保留缺失状态，不再自动追单 | 接受缺项关闭，不算全部下载 |

关键区别是：**正文与 SI 的获取状态、远程求助状态、用户是否关闭任务，必须分别记录。**

## 3. 交错处理，而不是一篇篇空等

```text
读取权威任务总表
    |
核对本地文件和已有求助，去重
    |
缺失正文批量发布（预算内）
    |
在正文等待期间处理 SI 的公开入口和独立求助
    |
定期集中收件，已到文件立即核对并回填
    |
报告完成项、待应助、缺失附件和完整性不明项
```

发布前记录意图，发布成功后记录求助 ID。若提交结果不明，先保留可能发生的积分支出，再核对远程记录。不能因为浏览器超时就再次扣分发布。

正文的既有请求不能阻止同一论文合法、明确的 SI 请求；但相同 SI 缺项不能重复发布。用户已指定只用科研通时，不强制再遍历全部公开渠道。

## 4. 中断后如何继续

```text
使用 $paper-si-workflow 从任务记录继续。
这次只收取已应助文件，不发布新请求，不增加积分支出。
不要改动已核对的文件；给我列出仍缺少的 SI。
```

助手应读取已有记录并核对相关远程状态，不把整批清单当作全新任务。遇到“发布结果不明”先核对，遇到已正确归档的文件跳过。每个新结果变化后保存记录。

## 5. 一个合格的交付摘要

以下仍是格式示例，不是项目实测成绩：

```text
正文：已核对 6 篇，待应助 1 篇。
SI：完整 1 篇；确认无 1 篇；部分 1 篇；
    完整性不明 1 篇；其他待处理或接受缺项 3 篇。
新增积分：按实际记录填写；未发生的支出不能计为已花费。
文件位置：链接至实际已保存且核对通过的文件。
需用户处理：登录、验证码、超预算或无法确认的文件身份。
```

“拿到了文件”和“拿全了附件”不是同一个指标。不能把审稿记录冒充实验 SI，不能把网页打不开解释成没有 SI，也不能因为用户同意结束任务就把所有行改成“已完成”。

## 6. 给第一次试用者的反馈问题

- 仅依照仓库说明，能否找到要安装的文件夹？
- 助手是否清楚说明当前工具和登录状态？
- 是否能在不覆盖旧文件的情况下复用正文和 SI？
- 是否分别记录正文、SI、待应助和发布结果不明？
- 没有完整附件列表时，是否避免声称全部 SI 已收齐？
- 中断再续接是否重复发布或重复支出？
- 实际人工介入几次，分别发生在哪一步？

请通过 [Issues](https://github.com/BinhuanQiu/paper-si-workflow/issues) 分享脱敏反馈。不要附密码、Cookie、登录状态、非公开论文或带令牌的下载链接。

## 演示边界

本页是公开的操作示例，不是真人用户评价、不证明端到端自动化已验证，也不提供外部资源访问权限。真实试用须由参与者在自己的合法访问条件下执行，记录客户端、工具版本、人工介入、耗时、积分和最终缺项。
